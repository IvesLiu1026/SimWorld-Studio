[FilterPlugin]
; Keep the machine-readable contract and artifact-manifest helper beside every
; packaged binary so an operator can independently compare the deployed
; artifact with the server allowlist. Tests and mutating install/build scripts
; intentionally stay outside the packaged plugin.
/Contract/...
/README.md
/Scripts/create-artifact-manifest.mjs
