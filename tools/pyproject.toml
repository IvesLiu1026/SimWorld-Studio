[project]
name = "simworld-asset-tools"
version = "0.1.0"
description = "Reproducible operational tooling for the SimWorld semantic asset stack"
requires-python = ">=3.10,<3.13"
dependencies = [
  "cryptography>=45,<47",
  "fastapi>=0.115,<1",
  "fastembed>=0.6,<1",
  "psycopg2-binary>=2.9.10,<3",
  "qdrant-client>=1.13,<2",
  "uvicorn>=0.34,<1",
]

[dependency-groups]
dev = [
  "jsonschema>=4.26,<5",
]

[tool.uv]
package = false
